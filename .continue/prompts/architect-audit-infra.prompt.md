Audit the infrastructure and local deployment setup.

Inspect:
- docker-compose
- service dependencies
- ports
- env variables
- health checks
- startup assumptions
- worker/broker/database connectivity

Classify issues as:
- prevents local development
- prevents reliable staging
- production risk only
